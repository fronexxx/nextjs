import React from 'react';
import Link from "next/link";

const Menu = () => {
    return (
        <div>
            <Link href={'cars'}>see all cars</Link>
        </div>
    );
};

export default Menu;
